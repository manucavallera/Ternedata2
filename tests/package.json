{
  "name": "ternedata-tests",
  "version": "1.0.0",
  "description": "Tests de integración contra producción",
  "scripts": {
    "test": "jest --runInBand --forceExit ternedata.test.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.4.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000,
    "verbose": true
  }
}
